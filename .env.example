# Add this in .env file 

# ML API URL - Your Flask backend URL (default: http://localhost:8080)
NEXT_PUBLIC_ML_API_URL = "http://localhost:8080"

# Note: Plant ID API is no longer used - we use our own ML model now
# NEXT_PUBLIC_PLANT_ID_API_KEY = "your-api-key-here"
